complete the autofill in the map (commit the changes), make the togglers in the dataview, compelete the autofill in the DataView, add the whole data. 
at this point you may send the app to carlos. 

then work in the backend. 

